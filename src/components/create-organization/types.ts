export type CreateOrganizationModalProps = {
  isModalOpen: boolean;
  onOpenModalChange: (open: boolean) => void;
};

export type UseCreateOrganizationModalProps = {
  onOpenModalChange: (open: boolean) => void;
};
